import { ComingSoon } from "@/components/ComingSoon";

export const metadata = { title: "Zones | ERCOT Large Load Tracker" };

export default function ZonesPage() {
  return (
    <ComingSoon
      title="Zone scorecards"
      description="Load-side queue trend, generation-side timelines, and the zone stress proxy, combined per ERCOT load zone with a transparent composite grade."
    />
  );
}
